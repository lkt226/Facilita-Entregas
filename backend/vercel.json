{
    "version": 2,
    "builds": [
        {
            "src": "src/index.ts",
            "use": "@vercel/node"
        }
    ],
    "build:types": "tsc --declaration --emitDeclarationOnly",
    "routes": [
        {
            "src": "(.*)",
            "dest": "src/index.ts"
        }
    ]
}